{
    "configurations": [
        {
            "name": "test sync client",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bazel-bin/example/helloworld/greeter_client",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
        }
    ]
}